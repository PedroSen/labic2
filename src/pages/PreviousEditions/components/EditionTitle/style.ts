import styled from "styled-components";

export const TitleH = styled.h1`
    font-family: var(--open-sans);
    font-size: 2.4rem;
    font-weight: 700;
    text-align: center;
    color: #FFFFFF;
    width: 100%;
`;